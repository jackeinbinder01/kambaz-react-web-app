export default function Lab1() {    // React function components implement algorithms that compute an HTML code 
    return (                        // snippet and return the result of the computation. Other functions can aggregate 
      <div id="wd-lab1">            // various snippets from different components into a larger, single HTML content 
        <h2>Lab 1</h2>              // that browsers can then render on the screen. Here the component is just returning
      </div>                        // a hard coded, static HTML code snippet. Later chapters will
  );}                               // introduce far more interesting algorithms to compute complex HTML